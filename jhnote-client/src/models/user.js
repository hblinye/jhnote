export class User {
    constructor(name, university, major, userKey){
        this.name = name
        this.university = university
        this.major = major
        this.userKey = userKey
    }

    fromData(data) {
        
    }
}